---
name: wish-e-commerce-pdp-skill
description: 设计、修改并交付长版或短版电商详情页。用于用户提供 PDF/Excel 策划稿、策划截图、右侧创意参考图、产品白底图、品牌资料、首屏 KV 或短版灰版，并要求制作分屏、前三屏、整套长图、立购台、定向修图或无文案信息版的任务。必须逐页读取策划，把对象准确数量、连接拓扑、中心轴、空间关系、跨屏接口和视觉 DNA 写成可验证合同；结构、视觉、内容三轮 QA 任一失败即阻断后续。短版锁定灰版原尺寸、两侧白边、位置和样式，只填充指定创意区域。
---

# Wish 电商详情页设计

把策划稿转成可验证、可批量延展的详情页视觉系统。不要把“严格按策划”停留在提醒层；先把要求转换为对象数量、连接关系、锚点、接口和视觉样式，再生成与验收。

## 核心原则

- 完整读取策划 PDF/Excel/截图的文字层与图像层。右侧参考图、箭头、批注、引线、场景案例和“参考右图”都属于需要检查的内容。
- 区分两层：保留策划指定的创意机制、动作、因果、数量、拓扑和证据布局；原创重构镜头、造型、材质、光线、配色、字体、Icon 和装饰表皮。
- 锁定产品身份。包装轮廓、瓶盖/泵头、标签层级、Logo、主色、SKU 数量、规格和关键包装文字以原始产品图为真值。
- 不新增卖点、数据、检测结论、机构、编号、印章、认证、脚注、规格或承诺。缺失时标记待确认。
- 把用户最新明确要求置于最高优先级。冲突顺序：用户最新要求 > 正式内容区 > 产品原始资产 > 策划备注 > 视觉参考 > 合理推断。
- 不把“元素都有”当成通过。对象数量、物理连接、中心轴、空间关系、设计质量和文字身份必须同时正确。
- 用户指出漏看策划、结构错位或整体美感不合格时，立即暂停后续屏，回到策划源和锁定合同重建；不要在错误基底上继续美化。

## 必须读取的参考

- 开始读取素材和建立屏序前，完整读取 [references/workflow-contract.md](references/workflow-contract.md)。
- 为每屏建立策划结构合同时，完整读取 [references/planning-contract-v2.md](references/planning-contract-v2.md)。
- 确认风格或重做任何已确认屏时，完整读取 [references/visual-dna-contract.md](references/visual-dna-contract.md)。
- 出现上下串联、同一场景延续、物体出入屏或长图过渡时，完整读取 [references/cross-screen-continuity.md](references/cross-screen-continuity.md)。
- 准备提示词、定向编辑或生产终稿时，完整读取 [references/prompt-templates.md](references/prompt-templates.md)。
- QA、返修和终稿放行前，完整读取 [references/qa-and-repair-matrix.md](references/qa-and-repair-matrix.md)。
- 合成长图时运行 `scripts/stitch_screens.py`。建立结构清单后运行 `scripts/validate_screen_manifest.py`。

## 路线与生成模式

先标记：

- `page_type=long`：多屏长版详情页，执行“策划确认 → 风格确认/锁定 KV → 前三屏 → 分组扩展 → 正式长图”。
- `page_type=short-grayboard`：短版/立购台，锁定灰版尺寸、两侧白边、模块位置和样式，只填充允许区域。
- `render_mode=concept`：方向探索、构图试验、创意样张。允许 ImageGen 一次性生成完整整屏，用于确认场景、材质、构图和视觉语言。
- `render_mode=production`：正式交付。场景与主视觉仍可由 ImageGen 完成；正式中文、数字、Logo、包装标签、报告和关键 Icon 优先使用用户提供或可确定复现的受保护资产。最终仍交付扁平 PNG，不要求可编辑分层文件。

若当前工具环境只能整屏 ImageGen：

1. 先按 `concept` 生成并确认构图。
2. 把正式文字、数字、包装与报告列为 `protected_assets`。
3. 每次编辑只改一个变量并逐字复核全部受保护内容。
4. 任一受保护内容漂移即失败，不以“整体差不多”放行。

## 阶段 0：素材预检

1. 标记每个输入角色：正式策划、策划右侧要求、产品身份、首屏 KV、短版灰版、品牌规范、风格参考或竞品灵感。
2. PDF 逐页提取文字并渲染全部页面；Excel 同时读取单元格、合并区域、嵌入图片和图片锚点；截图逐张查看。未检查完全部页，不得声称已理解策划。
3. 建立页码/区域到屏序的映射，记录识别到的屏数、正式内容顺序、右侧参考对应关系和素材缺口。
4. 向用户简短复述逐屏创意机制、核心构图和风格锚点。存在策划参考图时，未获得确认不得开始生成。

## 阶段 1：建立阻断型生产合同

为每屏建立结构化清单，至少包含：

- `screen_id`、`source_page_or_region`、`semantic_goal`、`exact_copy`
- `creative_mechanism`、`scene_layout`、`hero_action`
- `entity_count_exact`：每种关键对象的准确数量
- `connection_graph`：必须连接的对象及连接方向
- `anchor_positions`：关键对象的归一化位置或区域
- `shared_axes`：必须共轴、共中心线或对齐的对象
- `entry_exit_ports`：进入/离开本屏的物体、方向、坐标、尺寸与材质
- `primary_secondary_order`：第一、第二、第三视觉主体
- `evidence_presentation`、`protected_assets`
- `forbidden_entities`、`forbidden_substitutions`
- `style_dna_id`、`status`

使用 [references/planning-contract-v2.md](references/planning-contract-v2.md) 的字段和示例。将清单保存为 JSON 后运行：

```bash
python scripts/validate_screen_manifest.py --manifest <screen-manifest.json>
```

脚本失败时停止生成；不要以自然语言说明替代缺失字段。

## 阶段 2：锁定视觉 DNA

若用户提供已完成首屏 KV，原样作为第 1 屏成品和全套母版，不擅自重绘。若没有，生成三个在构图、材质、信息密度和视觉母题上真正不同的方向，等待用户选择。

把确认方向写为视觉 DNA 合同：

- 网格、边距、标题字体性格、切角/描边/立体深度、字号层级
- Icon 材质、厚度、透视、圆角、光线与颜色
- 产品尺度、角度、阴影和与场景的关系
- 核心对象材质，例如透明管道的透明度、折射、边缘高光、科技网格比例
- 场景真实/3D 比例、光线、背景虚化、信息密度和色彩比例
- 禁止降级项：普通默认黑体、普通圆贴、通用卡片阵列、材质过硬、蓝色过量等

每次重做整屏必须携带完整视觉 DNA；只写“沿用 B 方案”不合格。

## 阶段 3：生成与确认

### 长版

1. 先制作前三屏；每屏提示词同时携带生产合同、完整视觉 DNA、产品身份和受保护资产。
2. 输出三张独立预览和一张连续预览。前三屏未确认，不扩展剩余屏。
3. 剩余屏按相邻 2–3 屏分组推进。每次重新打开对应策划源，不凭记忆补通用画面。
4. 存在跨屏连续关系时，把相关屏作为一个组合设计与验收；单屏正确但拼接不连续仍判失败。

### 短版灰版

1. 把灰版作为像素级母版；锁定原宽高、两侧全高白边、模块坐标、圆角、间距、文字层级和样式。
2. 只填充灰色区、空白图片区或用户指定视觉区；策划指定的动作、对象数量和关系仍须落实。
3. 输出后与灰版叠加检查。白边、外轮廓、模块坐标或文字区明显漂移即失败。

## 阶段 4：三轮 QA 与一票否决

严格按顺序执行，上一轮未通过不得进入下一轮：

1. **结构与物理关系**：对象数量、连接图、中心轴、入口/出口、人物动作、液体流向、跨屏接口、禁止对象。
2. **视觉与设计质量**：视觉 DNA、标题与 Icon 设计语言、3 秒阅读、构图张力、空间层次、材质质量、前后屏节奏。普通黑体、普通圆贴、通用六宫格或“内容正确但没有美感”均不通过。
3. **内容与产品身份**：逐字文案、数字、Logo、规格、包装原生文字、报告来源；禁止虚构机构、编号、印章、脚注或额外结论。

任何一轮失败：

- 不得说“QA 已通过”。
- 不得继续下一屏或批量生成。
- 按 [references/qa-and-repair-matrix.md](references/qa-and-repair-matrix.md) 选择整屏重建、跨屏联改或受保护资产定向修正。

## 阶段 5：归档与交付

1. 仅把用户确认版本复制到干净的 `03-final-screens/`；旧稿和废稿不得混入。
2. 使用两位数序号命名并保持同宽，例如 `01-kv.png`。
3. 运行 `scripts/stitch_screens.py` 生成正式长图和拼接清单；检查缺屏、重复、白缝、宽度、总高度与跨屏接口。
4. 默认交付分屏、正式长图、清单和简短 QA 结果。只有用户明确要求才打包 ZIP。
5. 完整设计确认后询问是否需要无文案信息版。用户暂时不要时不要制作。
6. 无文案信息版只删除页面排版文字、数字、Icon 和说明；保留产品、包装原生文字、场景、人物/手部动作和既有画面结构。

## 强制返修规则

- 整体构图、对象数量、连接拓扑、主体动作或策划机制错误：从策划源整屏重建。
- 地漏/管道、人物/产品、液体/入口错位：重建物理拓扑，不做遮盖式修补。
- 跨屏过渡错误：相关屏同时修改并拼接验收。
- 标题/Icon/材质语言降级：重新加载完整视觉 DNA 后重建，不能只补一句“更有设计感”。
- 单个文字、数字、Logo、包装或报告错误：锁定其他区域，定向修正受保护资产；修后复核全部受保护内容。
- 报告无可靠原图：使用无机构、无编号、无印章的抽象证据卡，或标记待确认。
- 用户指出偏离策划：立即暂停后续屏，重新打开原策划与参考图再行动。
